node_modules
dist
.wrangler
.dev.vars
*.log

# D1 database dumps (wrangler d1 export) — contain real cadet data, never commit.
backup-*.sql

# Reference material dropped in the repo root, not app source.
*.docx
*.pdf

# Claude Code local tooling config — dev-server launch entries and Bash
# permission grants are per-developer, not project config to share.
.claude/
